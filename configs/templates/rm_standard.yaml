# Canonical reward model training template to stabilise preference pipelines.
experiment_type: rm
description: Reward model fine-tuning baseline with canonical hyperparameters.
per_device_train_batch_size: 32
gradient_checkpointing: false
save_steps: 500
logging_steps: 50
gradient_accumulation_steps: 1
learning_rate: 1e-5
max_source_length: 512
max_target_length: 48
